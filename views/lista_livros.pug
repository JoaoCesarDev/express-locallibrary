extends layout

block content
  h1= title

  ul
    each livro in book_list
      li
        a(href=livro.url) #{livro.titulo}
        |  #[strong (#{livro.autor.nome})]

    else
      li Não há livros